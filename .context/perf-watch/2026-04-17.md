# Perf Watch — 2026-04-17 05:39 UTC

## TL;DR

- Session nocturne : outillage live indisponible (sentry CLI absent, railway CLI
  non installé malgré hook session-start, host `facteur-production.up.railway.app`
  hors allowlist, Supabase CLI présent mais non authentifié).
- Aucune vérification Sentry/Railway/pool possible cette nuit. Rapport = snapshot
  statique du code + des PRs mergées sur 24h uniquement. **Pas de conclusion
  "all green" possible sans signal live.**
- 24h très denses côté PRs mergées sur `main` : 13 PRs dont **7 touchent le backend**
  (R3 pool fix, R4 feed 3→2 sessions, pertinence pillar, feed search, onboarding
  digest pre-gen, RSS parser v2, auth sentry breadcrumbs).
- Fixes Rounds 2/3/4 toujours présents dans `packages/api/app/database.py`
  (statement_timeout 30s, listener `_invalidate_on_supabase_kill`, pool 10+10).
  R1 (middleware request-budget) confirmé retiré en PR #412 (remplacé par
  timeouts endpoint-level dans `digest.py`).
- 0 fix proposé : impossible de justifier un changement sans données Sentry/logs
  à l'appui.

---

## 0. Alertes critiques

Aucune détectée — **mais absence de données ≠ absence d'incident**. Si un
`QueuePool limit` ou un `PendingRollbackError` s'est déclenché dans la fenêtre,
ce rapport ne peut pas le détecter. Priorité 1 = restaurer l'outillage (§9).

---

## 1. Cadrage

- **Fenêtre** : `[2026-04-16 05:39 UTC, 2026-04-17 05:39 UTC]`.
- **HEAD `origin/main`** : `b307995a` (`fix(feed): pull-to-refresh effectif en
  mode chronologique (#426)`, 2026-04-16 23:28 +0200 = 21:28 UTC).
- **Release backend la plus récente touchant l'API** : PR #425 `20aded90`
  (2026-04-16 23:10 +0200 = 21:10 UTC) — `dependencies.py` Sentry breadcrumbs.
- **PRs mergées dans la fenêtre (13 au total)** :

| PR | SHA | UTC merge | Scope | Backend ? |
|---|---|---|---|---|
| #414 | `0a7aa275` | 2026-04-16 08:52 | R3 pool fix (listener + per-user top3) | ✅ critique |
| #415 | `3d9c6f18` | 2026-04-16 10:59 | Mobile reader WebView | — |
| #416 | `9eade82e` | 2026-04-16 12:18 | Feed pertinence floor 0.5→0.2 | ✅ algo scoring |
| #417 | `cabc6272` | 2026-04-16 14:00 | **R4 feed 3→2 sessions** | ✅ critique |
| #418 | `1bef7516` | 2026-04-16 14:34 | Mobile FeedbackInline | — |
| #419 | `9742397d` | 2026-04-16 15:22 | Mobile digest UI | — |
| #420 | `29e5e2bf` | 2026-04-16 15:59 | Feed search chip + router | ✅ mineur |
| #421 | `beb554ba` | 2026-04-16 16:29 | Mobile sources search | — |
| #422 | `0d318d60` | 2026-04-16 20:49 | **Onboarding digest pre-gen BackgroundTasks** | ✅ à surveiller |
| #423 | `4687b859` | 2026-04-16 21:06 | Mobile feed preload/Hive cache | — |
| #424 | `bb2b0c41` | 2026-04-16 21:09 | **RSS parser v2 (+291 lignes httpx)** | ✅ à surveiller |
| #425 | `20aded90` | 2026-04-16 21:10 | Auth Sentry breadcrumbs | ✅ mineur |
| #426 | `b307995a` | 2026-04-16 21:28 | Feed chronological refresh | ✅ mineur |

Note : référence `7602d4f4` mentionnée dans le prompt pour R4 ne correspond pas
au SHA sur `main` (le merge R4 est `cabc6272`, PR #417). À clarifier pour le
prochain run (peut-être SHA squashé d'une branche de travail).

---

## 2. Sentry — synthèse

**Non récoltable ce run** : la CLI `sentry` mentionnée à `/Users/laurinboujon/.local/bin/sentry`
n'existe pas dans ce sandbox Linux (chemin macOS). Aucun MCP Sentry exposé via
`ToolSearch` non plus (seulement les MCPs GitHub). Variables d'environnement
`SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT` non définies.

Tableau §B non rempli. À re-exécuter avec outillage restauré.

---

## 3. Cross-check identité

Non applicable (pas de données Sentry). Rappel à maintenir pour le prochain run :
⚠️ Précédent "handoff iPhone/Dart" contredit par tags `browser=Chrome 147` / web
(cf. `bug-infinite-load-requests.md:648-650`). Ne jamais faire confiance à un
récit sans vérifier les tags Sentry live.

---

## 4. Railway — patterns

**Non récoltable ce run** : `railway` CLI absente (`which railway` → not found)
malgré le message du hook session-start "railway installé" — l'installation
officielle a échoué silencieusement (pas de binaire en PATH, pas de trace dans
`/root/.local/bin` ni `/usr/local/bin`). Lien `steadfast-hope / WEB` non
établi.

Patterns attendus à vérifier une fois l'outillage restauré :
- `db_connection_invalidated_by_signature` (listener R3 — `database.py:152`)
- `long_session_checkout` (observabilité Round 3 — `database.py:103`)
- `db_pool_pressure_high` (alerte ≥75% — `main.py:499`)
- `feed_phase1_context duration_ms`
- `scheduled_restart_initiated` (3×/jour Paris — `scheduler.py:120`)
- Restarts SIGTERM / OOM

---

## 5. Pool snapshot

**Non récoltable ce run** : `curl https://facteur-production.up.railway.app/api/health/pool`
→ `Host not in allowlist`. L'endpoint est unauth et prévu pour diagnostic externe
(cf. `main.py:465-467`) mais le sandbox réseau le bloque.

Valeurs du code à titre de référence (pas de lecture live) :
- `pool_size=10`, `max_overflow=10`, `pool_timeout=30`, `pool_recycle=180`
  (`database.py:52-58`).
- Seuil alerte `usage_pct ≥ 75 %` → log `db_pool_pressure_high` (`main.py:498-505`).

---

## 6. Hypothèses ranked

Toutes `confiance: faible` ou `non vérifiable` ce run — pas de data live.

### H1 — Le burst R4 (`/api/feed/` QueuePool) pourrait réapparaître

- **Preuves** : R4 (PR #417) déployée il y a ~15h. Bug doc §"Validation attendue"
  exige "0 événement `QueuePool limit` pendant 24 h" (`bug-infinite-load-requests.md:703`).
- **Confiance** : non vérifiable (aucun accès Sentry).
- **Verdict** : hypothèse non vérifiée, bloquant à valider demain.

### H2 — #422 `BackgroundTasks` sur `POST /users/onboarding` peut amplifier la pression pool sur un burst d'inscriptions

- **Preuves** : `users.py:111` ajoute `background_tasks.add_task(schedule_initial_digest_generation, ...)`.
  Le helper `_schedule_background_regen` est rate-limité + skip batch (cf. commit
  `0d318d60`), donc l'effet devrait être borné, mais chaque onboarding spawn
  **2 jobs** (normal + serein) qui utilisent `async_session_maker()`.
- **Confiance** : faible (une seule hypothèse, pas de data).
- **Verdict** : à surveiller via `/api/health/pool` pendant la journée si des
  onboardings ont lieu. Pas de fix proposé.

### H3 — #424 RSS parser v2 (+291 lignes httpx) pourrait augmenter latence ingestion

- **Preuves** : `rss_parser.py:211,269,287,317,...` nombreux `await client.get`
  en cascade pour auto-détection de feeds (fallbacks HTML index, curl-cffi, etc.).
  Chaque appel timeout=7s (`rss_parser.py:101`), mais pas de budget global sur
  la chaîne d'essais. Un site lent peut cumuler 5-7 × 7s = 35-49s.
- **Confiance** : faible (cadre d'usage = job ingestion, pas hot path user).
- **Verdict** : hypothèse non vérifiée. À mesurer via p95 `sync_service`
  si la CLI logs est restaurée.

---

## 7. Fix proposés

**Aucun fix proposé ce run** — la règle §5 du prompt ("pas de spéculation")
interdit de proposer un changement sans preuve. Les 3 hypothèses §6 sont
`confiance: faible` ou `non vérifiable`.

Recommandations d'observabilité (pas de modif code) :
1. Restaurer outillage sentry/railway (cf. §9).
2. Une fois Sentry live, vérifier sur 24h la signature `db_connection_invalidated_by_signature`
   + `QueuePool limit` + `long_session_checkout` + `db_pool_pressure_high`.
3. Une fois Railway logs accessibles, compter `scheduled_restart_initiated` et
   `digest_pre_generation_scheduled_on_onboarding` pour baseline.

---

## 8. Status Rounds précédents

| Round | Fix | État code | Fichier:ligne | Observation 24h |
|---|---|---|---|---|
| R1 | request-budget middleware | ❌ **retiré** (PR #412) | — | Remplacé par timeouts endpoint-level `digest.py` (30s/25s/60s), cf. `bug-infinite-load-requests.md:241` |
| R2 | `_short_session`, `statement_timeout=30000` | ✅ actif | `database.py:71`, multiples services | Non vérifiable (pas de logs) |
| R3a | Listener `_invalidate_on_supabase_kill` | ✅ actif | `database.py:141-156` | Non vérifiable |
| R3b | `top3_job` per-user session isolation | ✅ actif | `workers/top3_job.py` (PR #414) | Non vérifiable |
| R3c | `Semaphore(3)` extraction trafilatura | ✅ actif | `routers/contents.py:39` | Non vérifiable |
| R4 | Feed 3→2 sessions | ✅ actif (PR #417 `cabc6272`) | `services/recommendation_service.py` | **Validation 24h non vérifiable** (H1) |
| Bonus | `_scheduled_restart` 3×/jour | ✅ actif | `workers/scheduler.py:96,193,208` | À conserver (bug doc §R4 : "48h post-F1.1") |
| Bonus | `handle_error` checkout/checkin listeners | ✅ actif | `database.py:90-106` | Non vérifiable |
| Bonus | `get_db` rollback/close defensive | ✅ actif (PR #412) | `database.py:257-279` | Non vérifiable |

Aucune régression de code détectée au diff statique sur ces zones.

---

## 9. Suivis pour le dev humain

1. **[P0] Restaurer observabilité nocturne** — cette session est partiellement
   aveugle :
   - `scripts/setup-cli-tools.sh` mentionné par le hook session-start comme
     fallback — vérifier son existence et sa fiabilité.
   - Soit provisionner sentry-cli Linux dans l'image (binaire statique à
     `/usr/local/bin/sentry`), soit exposer le MCP Sentry déjà présent dans
     `mcp-servers/sentry/server.py` via `.mcp.json` du projet (token à passer
     côté secrets).
   - Ajouter `facteur-production.up.railway.app` à la WebFetch allowlist du
     sandbox pour `GET /api/health/pool` (endpoint public, unauth).
   - Même chose pour Railway CLI : le script `https://railway.app/install.sh`
     n'a pas déposé de binaire utilisable (retour hook "installé" trompeur).
2. **[P1] Vérifier 24h R4 (PR #417)** : une fois Sentry accessible,
   confirmer ≥24h sans `QueuePool limit` sur `feed.get_personalized_feed`
   (cf. critère `bug-infinite-load-requests.md:703-704`).
3. **[P2] Baseline #422 onboarding pre-gen** : compter sur 7 jours les
   occurrences `digest_pre_generation_scheduled_on_onboarding` vs `QueuePool limit`.
4. **[P2] Baseline #424 RSS parser** : timing p95 sur la route ingestion
   `RssParser` pour détecter les cascades de fallbacks > 20s.
5. **[P3] Clarifier SHA R4 dans le prompt agent** : référence `7602d4f4`
   dans la spec ne correspond pas à `cabc6272` (PR #417 sur `main`).

---

## 10. Notes / continuité pour demain

- Aucun rapport `.context/perf-watch/` antérieur trouvé dans ce sandbox
  (répertoire absent) — c'est soit le premier run, soit les artefacts ne sont
  pas persistés entre sessions. À vérifier côté harness.
- Aucun `/Users/laurinboujon/.claude/plans/` accessible (chemin macOS ; le sandbox
  est Linux, pas de mount de ce volume). Draft de plan non produit ce run
  (de toute façon aucune hypothèse `confiance: haute`).
- Pas de modification code/infra effectuée cette nuit (conforme §0 garde-fous).
- Fichiers lus : `database.py` complet, `main.py:440-519`, `bug-infinite-load-requests.md`
  §Round 3/4, `scheduler.py` (grep), `rss_parser.py` (grep), diffs PRs #414/#417/#422/#424/#425.
- À relancer demain en priorité une fois l'outillage §9.1 restauré — la fenêtre
  critique de validation R4 se terminera vers 2026-04-17 14:00 UTC.
