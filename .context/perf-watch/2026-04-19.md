# Perf Watch — 2026-04-19 05:32 UTC

## TL;DR

- **Run dégradé : aucune télémétrie collectée.** `sentry-cli` non authentifié (pas de `SENTRY_AUTH_TOKEN` dans l'env), `railway` CLI absent (install échouée au session-start, GitHub `403`), endpoint `/api/health/pool` injoignable (`Host not in allowlist` côté harness).
- Aucune décision Sentry / Railway / Supabase prise. Aucun fix proposé : sans evidence, toute proposition violerait la règle "evidence > narrative".
- Une seule PR mergée sur `main` dans la fenêtre 24h : **#436 `c2d2d802` "Round 5: backend FeedPageCache + mobile single-flight quick wins"** (mergée 2026-04-19 00:20 UTC). C'est la release actuelle en prod et un candidat de premier ordre à surveiller demain.
- Action humaine requise pour débloquer le watcher (cf. §9).

---

## 0. Alertes critiques

Aucune. (Et il faut le lire avec la prudence qui s'impose : sans Sentry ni Railway, l'agent est aveugle. "Aucune alerte" ici signifie "aucune alerte observable", pas "tout va bien".)

---

## 1. Cadrage

- Fenêtre : `[2026-04-18 05:32:43 UTC, 2026-04-19 05:32:43 UTC]`
- Branche d'investigation : `claude/elegant-bell-tXXlY` (worktree propre, aucun fichier modifié hors `.context/perf-watch/`).
- HEAD `origin/main` : **`c2d2d802`** — `Round 5: backend FeedPageCache + mobile single-flight quick wins (#436)` (auteur Laurin Boujon, 2026-04-19 02:20 +0200 = 00:20 UTC).
- PRs mergées sur `main` dans la fenêtre 24h : **1**
  - `c2d2d802` #436 — Round 5. Touche `packages/api/app/services/feed_cache.py` (nouveau, 183 LOC), `packages/api/app/routers/feed.py` (+138 LOC), invalidations dans `routers/{contents,custom_topics,personalization,sources,users}.py`, et côté mobile `feed_repository.dart` (+118 LOC), `feed_provider.dart` (+52). Total : 16 fichiers, +1200/-7. Description PR : "Per-user in-memory FeedPageCache (TTL 30s) with asyncio.Lock single-flight and invalidation on all feed-affecting mutations. Mobile debounce + SWR gate + cache-dedupe window. Fixes infinite-load amplification under >100 users."
- Aucun rapport `perf-watch/` antérieur dans ce repo : run d'amorçage. Pas de continuité à reprendre.

---

## 2. Sentry — synthèse

**Non collecté.** `sentry-cli info` :

```
Authentication Info:
  Method: Unauthorized
error: Auth token is required for this request. Please run `sentry-cli login` and try again!
```

`SENTRY_AUTH_TOKEN` n'est exposé dans aucune variable d'env du harness (`env | grep -i sentry` → vide). Aucune issue listée, aucun cross-check identité possible (§3 vide), aucune classification regression/pré-existant/noise.

---

## 3. Cross-check identité

Non réalisable sans accès Sentry. À faire dès le déblocage : prioriser les 3 culprits avec le plus d'events sur la fenêtre, vérifier `event.user.id` / `browser` / `release` pour distinguer trafic réel vs synthetic (rappel : précédent handoff "iPhone/Dart" était en réalité Chrome 147 web).

---

## 4. Railway — patterns

**Non collecté.** Le hook `session-start` rapporte :

```
[session-start] railway absent — tentative d'installation via script officiel...
[session-start] WARN: railway install failed (PATH issue? check ~/.railway/bin)
[...] curl: (22) The requested URL returned error: 403
```

Vérification post-install : `which railway` → not found ; `~/.railway/bin/` n'existe pas. Le script `scripts/setup-cli-tools.sh` repose sur `bash <(curl -fsSL https://railway.app/install.sh)` ; le `403` provient probablement d'une restriction réseau côté harness (cf. §9).

Aucun comptage `db_connection_invalidated_by_signature`, `long_session_checkout`, `db_pool_pressure_high`, `feed_phase1_context duration_ms=`, `digest_generation_timeout`, ni de comptage de restarts sur 24h. **Surveillance Round 4 (feed 3→2 sessions, `7602d4f4`) et Round 5 (FeedPageCache, `c2d2d802`) impossible cette nuit.**

---

## 5. Pool snapshot

**Non collecté.** `curl https://facteur-production.up.railway.app/api/health/pool` retourne `Host not in allowlist`. Le commit `c16f51da` "infra(cc-web): allow WebFetch to production health endpoint (#433)" laisse penser que l'allowlist est gérée pour le tool `WebFetch` mais pas pour le sandbox `Bash`/`curl` de cette session.

Aucune lecture de `status` / `usage_pct` / tendance possible.

---

## 6. Hypothèses ranked

Aucune hypothèse formulable sans evidence (Sentry IDs, log lines, snapshots pool). Toute hypothèse rédigée à partir du seul diff de `c2d2d802` serait de la spéculation et violerait la règle d'or §2.G.

**Hypothèse non vérifiée** (à confirmer demain) :
- **H0** : la PR #436 (`c2d2d802`) introduit `FeedPageCache` (TTL 30s, `asyncio.Lock` single-flight) et des invalidations dans 5 routers. Surface de risque : invalidation manquée → feed servi périmé ; `asyncio.Lock` per-user → sérialisation potentielle des requêtes concurrentes du même user ; cache mémoire → divergence inter-replica si Railway scale > 1 instance. Aucun de ces points n'est observé — seulement listé pour orienter le tri Sentry de demain (signatures à chercher : `feed_cache`, `FeedPageCache`, locks asyncio sur `routers/feed.py`).

---

## 7. Fix proposés

**Aucun.** Critères §2.H non satisfiables (pas d'hypothèse à confiance ≥ moyenne, pas d'evidence). En proposer un serait contraire aux garde-fous §0.

---

## 8. Status Rounds précédents

| Round | Fix | Présent dans le code (HEAD `c2d2d802`) ? | Signature Sentry attendue | Observation 24h |
|-------|-----|------------------------------------------|---------------------------|-----------------|
| R1 | request-budget middleware | Non vérifié ce run (fichier non lu pour économiser le contexte ; à confirmer demain) | n/a | **Non observable** (Sentry KO) |
| R2 | `_short_session`, `statement_timeout 30s` | Non vérifié ce run | `QueuePool limit`, `statement timeout` | **Non observable** (Sentry KO) |
| R3 | `handle_error` listener `_invalidate_on_supabase_kill`, `top3_job` per-user, `Semaphore(3)` trafilatura — commit `0a7aa275` (#414) | Présent (commit `0a7aa275` dans `git log` `database.py`) | `db_connection_invalidated_by_signature` | **Non observable** (Railway KO) |
| R4 | feed 3→2 sessions (`7602d4f4`) | Non vérifié ce run | `feed_phase1_context duration_ms` | **Non observable** (Railway KO) |
| R5 | FeedPageCache + mobile single-flight (`c2d2d802`, #436) — **NOUVEAU dans la fenêtre** | Présent (HEAD `main`) | À définir : signatures `feed_cache.*`, contention `asyncio.Lock`, invalidation manquée | **Non observable** ; à prioriser dès Sentry rétabli |

---

## 9. Suivis pour le dev humain

Sans intervention humaine, tous les runs suivants seront identiques à celui-ci (rapport vide). Par ordre de priorité :

1. **Exposer `SENTRY_AUTH_TOKEN` au harness** (variable d'env du conteneur Claude Code on the web, ou via `~/.sentryclirc` monté). Sans ça, §2/§3/§6/§7 restent vides.
2. **Réparer l'install Railway CLI dans `scripts/setup-cli-tools.sh`** : `curl -fsSL https://railway.app/install.sh` retourne `403` depuis ce sandbox. Pistes : (a) télécharger la release GitHub directement (`https://github.com/railwayapp/cli/releases/...`) comme c'est fait pour Supabase, (b) pré-installer dans l'image de base, (c) documenter le fallback `npm i -g @railway/cli`.
3. **Étendre l'allowlist `Bash`/`curl`** au domaine `facteur-production.up.railway.app` (ou au minimum à `/api/health/pool`). Aujourd'hui seul `WebFetch` semble autorisé (cf. PR #433), pas `curl` shell — donc §5 reste à 0.
4. **Vérifier que `RAILWAY_TOKEN` (ou login interactif équivalent) sera disponible** une fois le CLI réinstallé, sinon §4 restera vide même après fix de §9.2.
5. (Optionnel mais utile) Créer `.context/perf-watch/.gitignore` ou ajouter `.context/perf-watch/` à `.gitignore` racine si les rapports ne doivent pas remonter en PR — sinon préciser le contraire.

---

## 10. Notes / continuité pour demain

- Premier rapport généré par cet agent dans ce repo : aucun antécédent à référencer.
- HEAD `main` de référence à comparer demain : `c2d2d802` (Round 5).
- **Si l'environnement reste bloqué** : produire un rapport identique pointant ce fichier, ne pas dupliquer la liste des suivis, mais incrémenter un compteur "blocage J+N" en tête pour escalader.
- **Dès que Sentry est débloqué** : premier coup de filet sur 24h, comparer le baseline d'erreurs avant/après `c2d2d802` (00:20 UTC), focus sur `routers/feed.py` et `services/feed_cache.py`. Vérifier en particulier l'absence de signatures liées au `asyncio.Lock` per-user (deadlock, contention) et aux invalidations cross-router.
- **Dès que Railway est débloqué** : compter `db_connection_invalidated_by_signature` et `feed_phase1_context duration_ms` sur la même fenêtre pour valider la non-régression R3+R4.
- Aucune écriture hors `.context/perf-watch/2026-04-19.md`. Aucun draft de plan créé (`/Users/laurinboujon/.claude/plans/` n'existe pas dans ce conteneur Linux ; à recréer si l'agent doit y écrire, ou rediriger vers un chemin local).
