# Perf Watch — 2026-04-18 07:57 UTC

## TL;DR

- Aucune donnée live accessible depuis cet environnement : `sentry-cli` installé mais non authentifié, `railway` CLI absent (install 403), endpoint `/api/health/pool` bloqué (`Host not in allowlist`). Impossible d'exécuter les volets B, C, D, E du protocole.
- Côté code sur `origin/main` : les fixes R1→R4 sont toujours présents (pool 10+10, `pool_recycle=180`, `statement_timeout=30s`, listener `_invalidate_on_supabase_kill`, `db_pool_pressure_high`, `long_session_checkout`). Aucune régression structurelle introduite dans la fenêtre.
- Fenêtre `[2026-04-17T07:57Z, 2026-04-18T07:57Z]` : 5 commits sur `main`, tous infra/CLI/hooks/front web. Zéro commit backend hotspot (`database.py`, `main.py`, `scheduler.py`, `dependencies.py`, `recommendation_service.py`, `digest_service.py`). Surface de régression backend = nulle sur 24h.
- Rapport de type **"infra gap"** : aucune hypothèse de fix proposée — preuves insuffisantes, §0 interdit de spéculer.

---

## 0. Alertes critiques

Aucune confirmée faute de télémétrie accessible. **Gap de couverture** à signaler au dev humain : si une régression Sentry a eu lieu cette nuit, ce run ne l'a pas détectée.

---

## 1. Cadrage

- `now_utc` : `2026-04-18T07:57:32Z`.
- Fenêtre : `[2026-04-17T07:57:32Z, 2026-04-18T07:57:32Z]`.
- Release courante (HEAD `origin/main`) : `c16f51da` — `infra(cc-web): allow WebFetch to production health endpoint (#433)`.
- Rapport `.context/perf-watch/` précédent : **aucun** (premier run dans cet environnement — pas de continuité cross-session disponible).
- Plans `/Users/laurinboujon/.claude/plans/` : chemin inaccessible (sandbox Linux, pas le Mac de Laurin).

**PRs mergées dans la fenêtre (5)** :

| SHA | PR | Titre | Zone |
|---|---|---|---|
| `c16f51da` | #433 | infra(cc-web): allow WebFetch to production health endpoint | harness |
| `f29e64c9` | #432 | infra(mcp): document sentry MCP activation + readiness log | harness/doc |
| `153ae4e1` | #431 | infra(cli): provision sentry-cli alongside railway/supabase | harness |
| `54e8c582` | #430 | infra(hooks): honest install status for session-start | harness |
| `55a9f2f2` | #427 | fix: feed search UI — chip truncation + trending | mobile UI |

Aucune PR ne touche `packages/api/app/database.py`, `main.py`, `scheduler.py`, `dependencies.py`, ni les services `recommendation_service.py` / `digest_service.py`. Vérifié via `git log origin/main --since="24 hours ago" -- packages/api/app/...` : 0 résultat.

Dernier commit backend hotspot sur `main` : `cabc6272` — `fix(api): Round 4 — reduce /api/feed/ to 2 concurrent sessions (#417)`, hors fenêtre.

---

## 2. Sentry — synthèse

**Non exécutable.** `sentry-cli info` → `Method: Unauthorized`. Aucun token (`SENTRY_AUTH_TOKEN` absent de `env`, pas de `~/.sentryclirc`). L'environnement de ce run ne dispose d'aucun moyen d'interroger `facteur/python`.

Suggestion infra (cf. §9) : provisionner `SENTRY_AUTH_TOKEN` scopé `project:read` via secret injecté au SessionStart pour que le run nocturne fonctionne.

Tableau des issues : **vide** (non collectable).

---

## 3. Cross-check identité

Non exécutable — prérequis §2. Impossible de vérifier les tags `user.id` / `browser` / `geo` / `release` sur les culprits.

---

## 4. Railway — patterns

**Non exécutable.** `railway` absent du PATH. Le hook SessionStart confirme : `[session-start] WARN: railway install failed (PATH issue? check ~/.railway/bin)` puis `curl: (22) The requested URL returned error: 403` sur le script officiel. `which railway` → `not found`.

Patterns non collectés : `db_connection_invalidated_by_signature`, `long_session_checkout`, `db_pool_pressure_high`, `feed_phase1_context duration_ms`, `digest_generation_timeout`, `OOM`, `SIGTERM`, compteur de restarts 24h.

---

## 5. Pool snapshot

**Non exécutable.** `curl https://facteur-production.up.railway.app/api/health/pool` → `Host not in allowlist`. L'allowlist harness du PR #433 n'est pas propagée à ce sandbox (`WebFetch` oui, `curl` non).

Workaround possible (hors scope agent nocturne) : whitelister le host pour `Bash curl`, ou basculer le check sur `WebFetch`.

---

## 6. Hypothèses ranked

Aucune hypothèse formulable — §0 interdit la spéculation sans preuve. Les seules affirmations vérifiables dans cet environnement concernent l'état du code sur `main` :

**H-code1 — Fixes R1→R4 intacts sur `main` (confiance: haute).**
Preuves (extraites de `git show origin/main:packages/api/app/database.py`) :
- `packages/api/app/database.py:52` — `pool_size=10`
- `packages/api/app/database.py:53` — `max_overflow=10`
- `packages/api/app/database.py:55` — `pool_timeout=30`
- `packages/api/app/database.py:58` — `pool_recycle=180`
- `packages/api/app/database.py:71` — `"options": "-c statement_timeout=30000"` (R2)
- `packages/api/app/database.py:103` — log `long_session_checkout` (R2 `_short_session`)
- `packages/api/app/database.py:142` — `_invalidate_on_supabase_kill` (R3 listener)
- `packages/api/app/database.py:153` — log `db_connection_invalidated_by_signature` (R3)
- `packages/api/app/main.py:455` — handler `/api/health/pool`
- `packages/api/app/main.py:500` — log `db_pool_pressure_high`

Verdict : base structurelle stable sur 24h, aucun revert ni modif sur les fichiers sensibles.

---

## 7. Fix proposés

**Aucun.** Critère §0/§H : toute proposition doit adresser une hypothèse documentée par des preuves live. Preuves = 0 dans ce run. Proposer un fix ici serait de la spéculation.

Exception : §9 liste des **actions d'outillage** pour que le prochain run nocturne soit fonctionnel. Ce ne sont pas des fix applicatifs.

---

## 8. Status Rounds précédents

| Round | Fix | Présent sur `main` ? | Preuve code | Observation 24h |
|---|---|---|---|---|
| R1 | request-budget middleware | Partiel — commentaire `database.py:68` évoque le middleware, mais `cf882aae` (PR #412) dit `drop middleware`. À re-vérifier. | `database.py:68` (commentaire résiduel) | Non mesurable (pas de logs) |
| R2 | `_short_session` + `statement_timeout=30s` | Oui | `database.py:71` + `database.py:103` | Non mesurable |
| R3 | `handle_error` listener + per-user `top3_job` + semaphore(3) trafilatura | Oui (listener confirmé) | `database.py:142`, `database.py:153` | Non mesurable |
| R4 | feed 3→2 sessions concurrentes | Oui (PR #417 `cabc6272` mergée) | Pas de diff vérifié ici, mais log commit explicite | Non mesurable |

Régression sur fix actif : **non détectable** sans télémétrie. À traiter comme **inconnu**, pas comme "ok".

Point R1 à clarifier dans le prochain rapport : le commentaire `database.py:68` mentionne un "middleware request-budget" mais la PR #412 a explicitement drop un middleware. Lire `cf882aae` pour savoir si le commentaire est obsolète (cosmétique) ou si un autre middleware joue ce rôle.

---

## 9. Suivis pour le dev humain

Blocages infra qui rendent ce run stérile — à traiter hors agent :

1. **Sentry auth token absent**. Provisionner `SENTRY_AUTH_TOKEN` (scope `project:read` sur `facteur/python`) dans l'env du harness nocturne. Sans ça, §B/C du protocole sont morts.
2. **Railway CLI install 403**. Le script officiel renvoie 403 (cf. SessionStart log). Vérifier que le fallback `scripts/setup-cli-tools.sh` pointe vers une URL stable ou pin une version. Alternative : pré-baker le binaire dans l'image.
3. **Allowlist `curl` pour `facteur-production.up.railway.app/api/health/pool`**. PR #433 a ajouté l'host pour `WebFetch` uniquement. Étendre à `Bash curl` ou adapter le protocole §E à utiliser `WebFetch`.
4. **Continuité cross-session**. `.context/perf-watch/` était vide à l'arrivée. Si les runs précédents ont produit des rapports sur le Mac de Laurin, ils ne sont pas synchronisés ici. Confirmer que ce sandbox partage bien le worktree avec les runs précédents.
5. **Clarification R1**. Lire `cf882aae` (PR #412) pour trancher si le middleware request-budget est toujours actif ou s'il a été retiré et seulement les autres fixes le remplacent. Mettre à jour `docs/bugs/bug-infinite-load-requests.md` si ambiguïté.

---

## 10. Notes / continuité pour demain

- Si les points §9.1 et §9.3 sont réglés avant le prochain run, le protocole §B et §E redeviendront opérationnels sans Railway.
- Si §9.2 tarde, l'agent peut dégrader proprement : Sentry + pool snapshot suffisent pour produire un rapport signalétique ; les logs Railway sont le dernier volet.
- À relancer demain : `git log origin/main --since="24 hours ago"` pour détecter toute PR backend hotspot mergée dans la journée (ce run a eu 0 — pas représentatif).
- Aucun draft de plan écrit (§7 sans fix `confiance: haute` + §0 interdit les plans spéculatifs).
