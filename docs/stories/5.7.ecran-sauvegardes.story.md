# Story 5.7 : Écran "À consulter plus tard"

**As a** utilisateur,  
**I want** accéder à ma liste "À consulter plus tard",  
**so that** je puisse les consulter quand j'ai le temps.

## Acceptance Criteria

- [ ] Connecter liste 'À consulter' à endpoint API (soit nouveau endpoint, soit filtrage client/serveur)
- [ ] Affichage liste : same card format
- [ ] Tri par date d'ajout (plus récent en haut)
- [ ] Action "Retirer" (toggle bookmark)
- [ ] Empty State : "Aucun contenu à consulter"
- [ ] Navigation depuis BottomBar ou Settings (à définir selon UX, PRD dit "4.2... Bottom navigation bar", donc probablement un tab)

## Technical Notes

- Endpoint: `/api/feed/saved` ? Ou filtre sur `/api/feed` ?
- PRD 3.1 `user_content_status` exists.
- Flutter: `SavedScreen`, reuse `FeedCard`.
