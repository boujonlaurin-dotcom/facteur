# Story 5.7 : Écran Sauvegardés

**As a** utilisateur,  
**I want** accéder à mes contenus sauvegardés,  
**so that** je puisse les consulter plus tard.

## Acceptance Criteria

- [ ] Connecter liste 'Sauvegardés' à endpoint API (soit nouveau endpoint, soit filtrage client/serveur)
- [ ] Affichage liste : same card format
- [ ] Tri par date de sauvegarde (plus récent en haut)
- [ ] Action "Retirer" (toggle bookmark)
- [ ] Empty State : "Aucun contenu sauvegardé"
- [ ] Navigation depuis BottomBar ou Settings (à définir selon UX, PRD dit "4.2... Bottom navigation bar", donc probablement un tab)

## Technical Notes

- Endpoint: `/api/feed/saved` ? Ou filtre sur `/api/feed` ?
- PRD 3.1 `user_content_status` exists.
- Flutter: `SavedScreen`, reuse `FeedCard`.
