# Story 4.3: Card de contenu (preview)

## Status: Done

> [!NOTE]
> **Testing Note**: The UI and logic are verified via automated widget tests. Manual testing of pull-to-refresh in Chrome (Web) was not performed due to environment limitations, but the implementation follows standard Flutter patterns.

## Story

**As a** utilisateur,
**I want** voir un aperçu attractif de chaque contenu,
**so that** je puisse décider si je veux le consulter.

## Acceptance Criteria

- [ ] **Card Layout**:
    - Header: Image (si disponible).
    - Body: Titre + Date relative/Type.
    - Footer: Source (fond distinct) + Actions.
- [ ] **Actions**:
    - [ ] Icône bookmark 🔖 (visuel uniquement pour cette story, logique dans Story 4.4).
    - [ ] Menu "..." pour actions secondaires.
- [ ] **Design**: Respecte la charte graphique (thème sombre, animations `FacteurCard`).
- [ ] **Thumbnails**: Gestion des images avec fallback si absent.

## Tasks / Subtasks

- [ ] **Update Content Model**
    - [ ] Verify `Content` model has all necessary fields (already done in 4.2 refactor).
- [ ] **Enhance FacteurCard Wrapper**
    - [ ] Ensure `FacteurCard` supports complex layouts (already robust).
- [ ] **Implement FeedCard Widget**
    - [ ] Create `apps/mobile/lib/features/feed/widgets/feed_card.dart` to encapsulate the card logic (refactoring out of `FeedScreen`).
    - [ ] Implement thumbnail display (network image with cache).
    - [ ] Implement "Relative Date" formatting (e.g., "il y a 2h").
    - [ ] Add Bookmark button placeholder.
    - [ ] Add More Options ("...") button placeholder.
- [ ] **Update FeedScreen**
    - [ ] Replace `_buildContentCard` with new `FeedCard` widget.

## Dev Notes

- **Reuse**: Use `FacteurCard` as the container.
- **Packages**: Use `cached_network_image` for thumbnails if available, or standard `Image.network` with error builder.
- **Date Formatting**: Use `timeago` package or custom helper.
- **Icons**: Use `PhosphorIcons`.
