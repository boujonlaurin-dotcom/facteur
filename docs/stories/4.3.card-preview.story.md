# Story 4.3: Card de contenu (preview)

## Status: Done

> [!NOTE]
> **Testing Note**: The UI and logic are verified via automated widget tests. Manual testing of pull-to-refresh in Chrome (Web) was not performed due to environment limitations, but the implementation follows standard Flutter patterns.

## Story

**As a** utilisateur,
**I want** voir un aperçu attractif de chaque contenu,
**so that** je puisse décider si je veux le consulter.

## Acceptance Criteria

- [x] **Card Layout**:
    - Header: Image (si disponible).
    - Body: Titre + Durée (si présent).
    - Footer: Source (Logo + Nom) + Récence + **Badge Transparence** + Actions.
- [x] **Actions**:
    - [x] Icône bookmark 🔖 (Logique gérée via `FeedProvider`).
    - [x] Menu "..." pour actions secondaires (Masquer, Signaler).
- [x] **Transparence** : Affichage discret de la raison de recommandation (ex: "Sujet passionnant : Tech").
- [x] **Design**: Respecte la charte graphique (thème sombre, animations `FacteurCard`).
- [x] **Thumbnails**: Gestion des images avec fallback si absent.

## Dev Notes

- **Footer Layout** : Utilisation de `Flexible` (loose) et `Expanded` pour permettre à la source et au badge de cohabiter dynamiquement sans vide inutile. Priorité de flexibilité donnée au badge pour éviter sa troncation prématurée.
- **Clean UI** : L'icône du type de contenu n'est affichée que pour les contenus non-textuels (Audio/Vidéo) pour éviter l'encombrement visuel.
- **Wording** : Libellés de recommandation générés par le backend en français, sans majuscule excessive.
