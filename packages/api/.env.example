# Facteur API - Configuration

# Application
ENVIRONMENT=development
DEBUG=true

# Server
HOST=0.0.0.0
PORT=8000

# Database — local Postgres (docker-compose.test.yml, also used for dev)
# Creds match POSTGRES_TEST_USER / POSTGRES_TEST_PASSWORD / POSTGRES_TEST_DB
# in the repo-root .env. Run `make bootstrap` to start the container + migrate.
DATABASE_URL=postgresql+psycopg://facteur:facteur@localhost:54322/facteur_test

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# RevenueCat
REVENUECAT_API_KEY=your-revenuecat-api-key
REVENUECAT_WEBHOOK_SECRET=your-webhook-secret

# RSS Sync
RSS_SYNC_INTERVAL_MINUTES=30
RSS_SYNC_ENABLED=true

# CORS (comma-separated origins)
CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]

# Sentry (production only)
SENTRY_DSN=

# PostHog (Story 14.1 — retention cohorts)
POSTHOG_API_KEY=
POSTHOG_HOST=https://eu.i.posthog.com
POSTHOG_ENABLED=false
# Comma-separated emails (case-insensitive) for special cohorts
POSTHOG_CREATOR_EMAILS=
POSTHOG_CLOSE_CIRCLE_EMAILS=

# Admin endpoints shared secret (required for /api/admin/*)
ADMIN_API_TOKEN=
