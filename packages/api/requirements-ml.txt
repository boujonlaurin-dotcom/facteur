# Optional ML dependencies (enable only if ML_ENABLED=true)
numpy>=1.26.0,<2.0
transformers==4.38.2
torch==2.2.0
spacy==3.8.11

# spaCy French model (install separately):
# python -m spacy download fr_core_news_md
